language: java
jdk:
  - oraclejdk8
  - oraclejdk7
  - openjdk7
#  - openjdk6 # build fails with openjdk6

install: mvn -B dependency:resolve dependency:resolve-plugins -DexcludeReactor=true -DexcludeGroupIds=org.apache.cxf > /dev/null

script: mvn -B -Ptravis test
